---
name: obsidian-enrich-note
description: "Обогащение frontmatter заметки — aliases, up, down, other, summary без перемещения файла. Триггеры: «обнови/заполни frontmatter», «добавь aliases/связи/summary», «обогати заметку ссылками». Для рефакторинга с перемещением — obsidian-refactor-inbox."
---

# obsidian-enrich-note

Точечное обновление frontmatter **без перемещения файла и без изменения тегов**. Меняются только: `aliases`, `up`, `down`, `other`, `summary`. Тело заметки, теги, `links`, `sources` — не трогаем.

Семантика полей `up`/`down`/`other`/`links` — в `.agents/rules/content-style.md`. Структура хранилища — в `.agents/rules/vault-struct.md`.

## Когда НЕ использовать

| Ситуация | Используй вместо |
|----------|-----------------|
| Нужно сменить теги и переместить заметку | `obsidian-refactor-inbox` |
| Сначала понять, что в инбоксе | `obsidian-inbox-review` |
| Заметку нужно разбить | `obsidian-split-note` |

---

## Алгоритм

### 1. Прочти заметку

Зафиксируй главную тему и ключевые концепции из заголовка и тела.

### 2. Составь aliases

Думай как поисковый запрос пользователя:

- Синонимы понятия на русском
- Английский эквивалент (если термин устоявшийся)
- Аббревиатуры
- Альтернативные написания

```yaml
aliases:
  - Закон Гудхарта
  - Goodhart's Law
  - Goodhart Law
```

### 3. Найди связи

Сначала — **семантический поиск** через `mcp__obsidian-hybrid-search__myvault_search`. Он ловит заметки про ту же тему, написанные другими словами, чего `rg` не делает. `queries`: [главная тема заметки на русском, английский эквивалент термина]. Параметры гибридного поиска — см. CLAUDE.md.

Из результатов раскидывай:

- MOC из `03. Ресурсы/07. Карты/` → `up`
- Атомарки из `03. Ресурсы/04. Заметки/` по смежным темам → `other`
- Заметки про людей из `02. Сферы/01. Люди/` → `other`
- Заметки, для которых эта — родительская тема → `down`

Если по теме известен конкретный термин (имя инструмента, аббревиатура) — добавь точечный лексический поиск:

```bash
rg -l "KEYWORD" "03. Ресурсы/" "02. Сферы/01. Люди/" | head -20
```

### 4. Заполни поля

Правила (из `content-style.md`):

- `up` — родительская тема / MOC, из которой вытекает заметка (от частного к общему)
- `down` — что эта заметка порождает (от общего к частному)
- `other` — горизонтальные связи (смежные темы, люди, MOC, не подходящие под up/down)

Пустое поле — норма, если связи нет. Не выдумывай.

### 4.1. Заполни summary (если заметка знаниевого типа)

Если поле `summary` у знаниевого типа отсутствует или пустое — заполни по политике `summary` в `note-types-frontmatter.md` (там же — стиль, scope по типам, кавычки, критерий `#needs-split`). Контейнерным типам не добавляй. Непустой `summary` не перезаписывай — при явном противоречии телу вынеси в отчёт, решает пользователь. До-генерация `summary` **не** ставит `ai_generated`.

### 5. Обнови frontmatter через Edit

Если поле уже заполнено — **дополняй**, не перезаписывай. Wikilinks — в кавычках если содержат двоеточие или начинаются с `[[`:

```yaml
aliases:
  - Синоним 1
  - English Name
up:
  - "[[Родительская тема]]"
down:
  - "[[Дочерняя тема]]"
other:
  - "[[Смежная тема]]"
  - "[[Имя Человека]]"
```

---

## Чек-лист

- [ ] `aliases` непустые (хотя бы один)
- [ ] Использован `myvault_search` для поиска семантических соседей
- [ ] `up` — wikilinks или обоснованно пуст
- [ ] `down` — wikilinks или обоснованно пуст
- [ ] `other` — wikilinks (часто здесь больше всего связей)
- [ ] Все wikilinks указывают на существующие файлы
- [ ] `summary` заполнен для знаниевого типа (или обоснованно пропущен для контейнерного); у проектов (`#project`) вместо него заполнен `description`
- [ ] Файл остался в исходной папке
- [ ] Теги, тело, `links`, `sources` не изменились
